// Used for local development (`ng serve`). The production build swaps this file
// for environment.prod.ts via the "fileReplacements" entry in angular.json.
export const environment = {
  production: false,
  apiBase: 'http://127.0.0.1:8000',
};
