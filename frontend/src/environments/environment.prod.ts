// Used for the production build (the one Vercel deploys). Replace apiBase with
// your actual Render backend URL once it's deployed — see docs/deployment.md.
export const environment = {
  production: true,
  apiBase: 'https://bankapp-backend-nboo.onrender.com',
};
